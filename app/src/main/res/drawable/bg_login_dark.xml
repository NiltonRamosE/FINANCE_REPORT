<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Capa base: navy profundo -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/navy_900" />
        </shape>
    </item>
    <!-- Overlay radial suave: usar en código con Canvas o con ShapeDrawable -->
    <!-- Para efecto de "glow" en esquina superior derecha, aplica en código:
         val paint = Paint().apply {
             shader = RadialGradient(
                 width.toFloat(), 0f, width * 0.8f,
                 Color.parseColor("#1A1E4D8C"), Color.TRANSPARENT,
                 Shader.TileMode.CLAMP
             )
         }
    -->
</layer-list>